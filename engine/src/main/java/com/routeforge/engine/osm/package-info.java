/**
 * OSM (OpenStreetMap) data ingestion.
 * <p>
 * In Phase 0 we only count nodes/ways/relations from a {@code .osm.pbf} file.
 * In Phase 1 the loader will be extended to build the road graph.
 */
package com.routeforge.engine.osm;
